%h2(style="margin-top: 0; text-transform: uppercase")= @page.title
%div(style="max-width: 560px; font-size: 16px")
  = raw @page.content

  %p
    = link_to 'Edit', edit_page_path(@page)
    \|
    = link_to 'Back', pages_path

- content_for :title do
  = @page.title